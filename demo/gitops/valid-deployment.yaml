apiVersion: apps/v1
kind: Deployment
metadata:
  name: valid-nginx
  namespace: kyverno-demo
  labels:
    app: valid-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: valid-nginx
  template:
    metadata:
      labels:
        app: valid-nginx
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: nginx
          image: nginxinc/nginx-unprivileged:1.27.4-alpine
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            runAsNonRoot: true
            runAsUser: 101
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
          ports:
            - containerPort: 8080
